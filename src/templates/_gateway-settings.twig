{% from "_includes/forms" import booleanMenuField %}

{{ booleanMenuField({
    label: "Only allow this gateway to be used for zero value orders?"|t('commerce'),
    name: 'onlyAllowForZeroPriceOrders',
    includeEnvVars: true,
    errors: gateway.getErrors('onlyAllowForZeroPriceOrders'),
    value: gateway.getOnlyAllowForZeroPriceOrders(false),
}) }}

<p>{{ 'Bank accounts, formats and references are configured in Settings → Plugins → Bank Transfer Payment Codes.'|t('bank-transfer-payment-codes') }}</p>
